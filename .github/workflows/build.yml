name: Build Chimera Binary

on:
  # Trigger on release creation
  release:
    types: [created]
  
  # Allow manual trigger from GitHub Actions tab
  workflow_dispatch:
    
  # Optionally trigger on specific tags
  push:
    tags:
      - 'v*'  # Matches v1.0.0, v2.1.0, etc.

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    name: Build Binaries
    runs-on: ubuntu-latest
    strategy:
      matrix:
        target: [i686-unknown-linux-musl, x86_64-pc-windows-gnu]
        include:
          - target: x86_64-pc-windows-gnu
            extension: .exe
          - target: i686-unknown-linux-musl
            extension: ""

    steps:
    - uses: actions/checkout@v4
    
    - name: Install Rust
      uses: dtolnay/rust-toolchain@stable
      with:
        targets: ${{ matrix.target }}
    
    - name: Install cross
      run: cargo install cross --git https://github.com/cross-rs/cross
    
    - name: Build Target
      run: |
        cd chimera
        cross build --target ${{ matrix.target }} --release
    
    - name: Upload artifacts
      uses: actions/upload-artifact@v4
      with:
        name: chimera-${{ matrix.target }}
        path: chimera/target/${{ matrix.target }}/release/chimera${{ matrix.extension }}

    - name: Upload Release Assets
      if: github.event_name == 'release'
      uses: softprops/action-gh-release@v1
      with:
        files: chimera/target/${{ matrix.target }}/release/chimera${{ matrix.extension }}
