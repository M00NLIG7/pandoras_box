use crate::net::spread::spreader::{Spreader, OS};

fn init() {
    // let mut spreader = Spreader::new();
    // spreader.add_pool(OS::Unix, todo!());
    // spreader.add_pool(OS::Windows, todo!());
    // spreader.spread(todo!());
}
