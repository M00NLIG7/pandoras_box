[workspace]
members = [
    "pandoras_box",
    "rustrc",
    "rustrc/download_embed_macro",
    "chimera",
]

resolver = "2"

[profile.release]
opt-level = "z"
strip = true
lto = true
codegen-units = 1
panic = "abort"
